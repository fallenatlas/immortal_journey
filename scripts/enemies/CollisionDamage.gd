extends Area2D

func _on_body_entered(body):
	#if body.name == "Player":
		#Game.take_damage(3)
	pass

#func _process(delta):
	#var bodies = get_overlapping_bodies()
	#for body in bodies:
		#if body.name == "Player":
			#Game.take_damage(3)
